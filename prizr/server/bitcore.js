var bitcore = Meteor.npmRequire('bitcore');

bitcoin = {
    test: function() {
        return 'bitcore test is good';
    }
};

//var privateKey = new PrivateKey();
//var address = privateKey.toAddress();